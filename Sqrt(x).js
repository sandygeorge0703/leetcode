/**
 * @param {number} x
 * @return {number}
 */


var mySqrt = function (x) {
  // Calculate the square root using Math.sqrt and return the integer part using Math.floor
  return Math.floor(Math.sqrt(x));
};

// Example usage:
console.log(mySqrt(4)); // Output: 2
